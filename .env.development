# .env.development
# Override only the values that need to be different for development
VITE_API_BASE_URL=/api/v1
VITE_DEV_PROXY_TARGET=http://localhost:8101
VITE_APP_ENV=development
VITE_DEBUG_MODE=true
