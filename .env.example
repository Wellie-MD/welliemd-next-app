# DTC and employer deployments point to the isolated tenant runtime.
# The dedicated corporate_operator deployment points to the IS_ADMIN=true control plane.
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_PORTAL_MODE=dtc
VITE_CORPORATE_PILOT_ENABLED=false
