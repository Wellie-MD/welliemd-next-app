# Client and admin
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Optional — auto-derived from window.location in standard environments:
#   localhost          → http://localhost:3000
#   admin.X.com        → https://intake.X.com
#   admin-staging.X.com → https://intake-staging.X.com
# Set this only for non-standard/custom intake app deployments.
# VITE_QUESTIONNAIRE_PREVIEW_BASE_URL=https://intake.example.com

VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
